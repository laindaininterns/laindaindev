fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
redis==5.0.8
# Pinned to 1.2.1, not older. Versions before this pull in av<13, which
# has no prebuilt Windows wheel for recent Python versions and fails to
# compile without FFmpeg dev headers installed. 1.2.1+ accepts av>=11
# with no upper bound, which resolves to a build with real wheels.
faster-whisper==1.2.1
python-multipart==0.0.9
tenacity==9.0.0

# dev / test only
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
