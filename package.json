{
  "name": "lain-dain-wholesale-platform",
  "version": "1.0.0",
  "private": true,
  "description": "LainDain Wholesale Marketplace Platform (Client & Server)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "start:server": "npm --prefix server start",
    "build:client": "npm --prefix client run build"
  },
  "devDependencies": {
    "agentation": "^3.0.2"
  },
  "dependencies": {
    "posthog-js": "^1.417.1"
  }
}
